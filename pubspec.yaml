name: async_content
description: A library that helps create screens that load content and work with a view model.
version: 1.1.1
# repository: https://github.com/my_org/my_repo
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  extension_flutter_tools: ^1.6.1
  extension_dart_tools: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  test: ^1.21.0
